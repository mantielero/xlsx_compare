name: Builder
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:

  tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        nimversion:
          - binary:stable
        os:
          - ubuntu-latest
          #- windows-latest

    steps:
      - run: echo "Build tool"
      - run: nim c -d:release src/xlsx_compare
